/dist
/node_modules
/public

# Local env (secrets — never commit). Copy .env.example → .env
.env
.env.*
!.env.example

# Smithers run store (ephemeral — never commit)
smithers.db
smithers.db-shm
smithers.db-wal

# Local dev runtime artifacts (never commit)
.run/
logs/
server/data/
server/storymap-server

# Internal tooling / project management (not part of the app)
.smithers/
delegation/
handout-delegation.html
audio-data-colleciton

# Internal docs (removed from the public repo)
GITHUB_SSO_SETUP.md
feature_request_audio_playback.md
feature_request_user_created_storymap.md
AGENTS.md
